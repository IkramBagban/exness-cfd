# Production Environment Variables
# Copy this file to .env.production and update with your production values

# Backend API URL (update with your production domain)
VITE_API_URL=https://api.yourdomain.com

# WebSocket URL (update with your production domain)
VITE_WS_URL=wss://ws.yourdomain.com

# Note: In Docker production, these will typically be:
# VITE_API_URL=http://backend:3000 (if using internal Docker networking)
# VITE_WS_URL=ws://ws:8080 (if using internal Docker networking)
# 
# Or if using reverse proxy (recommended):
# VITE_API_URL=https://yourdomain.com/api
# VITE_WS_URL=wss://yourdomain.com/ws
